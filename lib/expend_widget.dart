import 'package:flutter/material.dart';

class ExpendWidget extends StatelessWidget {
  const ExpendWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column());
  }
}
